# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Local data (now using HF datasets, not tracked in git)
data/

# Model weights / training artifacts (regenerated on each run)
*.pt
*.npz

# TensorBoard event logs
runs/

# Saved config copies (generated alongside checkpoints, for reproducibility)
*_config.yaml
